<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="deom.jingdong.wwx">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 二维码 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="utils.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TranslucentTheme">
        <activity
            android:name=".activity.activity.MainActivity"
            android:theme="@style/TranslucentTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.activity.HomeActivity"
            android:theme="@style/TranslucentTheme" />
        <activity
            android:name=".activity.activity.WebActivity"
            android:theme="@style/TranslucentTheme" />
        <activity
            android:name="resou.ReSouActivity"
            android:theme="@style/TranslucentTheme" />
        <activity android:name=".activity.activity.SearchActivity" />
        <activity android:name=".activity.activity.CustomCaptrueActivity" />
        <activity android:name=".activity.activity.XiangQingActivity" />
        <activity android:name=".activity.activity.LoginActivity" />
        <activity android:name=".activity.activity.GeRenActivity" />
        <activity android:name=".activity.activity.ResActivity" />
        <activity android:name=".activity.activity.DingDanActivity" />
        <activity android:name=".activity.activity.QueRenActivity" />
        <activity android:name=".activity.activity.GouWuCheActivity" />
        <activity android:name=".activity.activity.ImageActivity"></activity>
    </application>

</manifest>